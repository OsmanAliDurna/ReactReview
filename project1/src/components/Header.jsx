import HeaderStyle from "../scss/header.module.scss";

const Header = () => {
  return (
    <header>
      <h1 className={HeaderStyle.header}>COMMENTS</h1>
    </header>
  );
};

export default Header;
